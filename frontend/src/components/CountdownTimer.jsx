// Displays a live countdown to the auction end time.
export default function CountdownTimer({ endsAt }) {
  // TODO: compute and tick down the remaining time until endsAt.
  return <span className="countdown">{/* TODO */}</span>;
}
