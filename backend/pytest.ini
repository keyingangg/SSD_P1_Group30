[pytest]
DJANGO_SETTINGS_MODULE = securebid.settings.development
python_files = tests/test_*.py
# All real tests live in testing/ at the repo root. This config is kept so
# running pytest from backend/ still works for local exploration, but CI
# always runs from testing/.
addopts = --ignore=venv --collect-only -q
